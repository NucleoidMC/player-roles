{
  "schemaVersion": 1,
  "id": "player_roles",
  "version": "${version}",
  "name": "Player Roles",
  "description": "Role management & permissions for Fabric servers via a JSON file",
  "authors": ["Gegy"],
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
    "main": ["dev.gegy.roles.PlayerRolesInitializer"]
  },
  "mixins": ["roles.mixins.json"],
  "depends": {
    "fabricloader": ">=0.8.9",
    "fabric": "*",
    "minecraft": "1.16.x"
  }
}
